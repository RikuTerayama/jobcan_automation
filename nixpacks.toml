[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
    "python -m venv --copies /opt/venv",
    ". /opt/venv/bin/activate && pip install --no-cache-dir --disable-pip-version-check flask==3.0.0 gunicorn==21.2.0"
]

[start]
cmd = "bash start.sh" 
